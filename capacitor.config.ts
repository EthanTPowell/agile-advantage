import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Davesa Support',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
